interface:
  display_name: "SuperHUA"
  short_description: "Run-scoped Superteam workflow."
  default_prompt: "Use $superhua to create a run-scoped proposal and high-level design under working/superhua-runs/<run-id>/, wait for approval at each gate, then continue with planning and implementation."
